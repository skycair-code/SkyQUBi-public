[General]
# S7 SkyQUBi SDDM theme — placeholder
# Canonical palette from /s7/skyqubi-private/branding/palette.json
# A full Qt-based SDDM theme lives in this dir once someone builds one;
# for now this minimal config is enough for SDDM to load the theme
# without errors. Falls back to SDDM's default rendering with S7's
# background color.
type=color
color=#1a0f1c
background=
fontSize=10
